<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8"/>
    <title>{{ pageTitle }}</title>
  </head>
  <body>
    {{> header }}
    <p>{{ maintenanceMessage }}</p>

    {{> footer }}
  </body>
</html>
